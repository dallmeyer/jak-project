;;-*-Lisp-*-
(in-package goal)

(defmacro mod-cave-run-each-frame ()
  `(begin
    (mod-move-ecovent "ecovent-253" (static-vector-meters 1080.19  25.94 -3269.64)) ;; yellow - moved to center island in entrance so you can reach secret cell in sage hut
    )
  )

  
(defmacro mod-cave-override-init-position (obj arg0)
  `(case (-> obj name) :comp name=
    ;; ORBS
    

    ;; FLIES
    
    
    ;; CELLS
    
    
    ;; MISC
    
    )
  )